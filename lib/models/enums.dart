enum NodeRole { normal, relay, lowPowerRelay, dormant }
enum MessagePriority { normal, important, critical }
enum BroadcastZone { all, radius1km, radius5km, radius10km }
enum TransferState { pending, transferring, paused, completed, failed }
